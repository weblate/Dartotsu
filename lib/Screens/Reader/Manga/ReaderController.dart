import 'package:flutter/material.dart';

import 'Reader.dart';

class ReaderController extends StatefulWidget {
  final Reader reader;

  const ReaderController({super.key, required this.reader});

  @override
  ReaderControllerState createState() => ReaderControllerState();
}

class ReaderControllerState extends State<ReaderController> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
